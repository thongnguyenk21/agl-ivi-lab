# Local Artifacts

This directory contains files that are required for local experiments but are
too large or environment-specific for Git.

- `data/` contains datasets, samples, and generated input.
- `models/` contains model weights and compiled inference engines.

Only placeholder files are committed. Future download scripts must record the
source, version, checksum, and license of every external artifact.
